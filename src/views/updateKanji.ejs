<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Kanji Character</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="mb-4">Edit Kanji Character</h1>
      <form action="/kanji/update/<%= kanji.id %>" method="POST">
        <div class="mb-3">
          <label for="kanji_character" class="form-label"
            >Kanji Character</label
          >
          <input
            type="text"
            class="form-control"
            id="kanji_character"
            name="kanji_character"
            value="<%= kanji.kanji_character %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="readings_on" class="form-label">On Readings</label>
          <input
            type="text"
            class="form-control"
            id="readings_on"
            name="readings_on"
            value="<%= kanji.readings_on %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="readings_kun" class="form-label">Kun Readings</label>
          <input
            type="text"
            class="form-control"
            id="readings_kun"
            name="readings_kun"
            value="<%= kanji.readings_kun %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="meaning" class="form-label">Meaning</label>
          <textarea
            class="form-control"
            id="meaning"
            name="meaning"
            rows="3"
            required
          >
            <%= kanji.meaning %>
            </textarea
          >
        </div>
        <button type="submit" class="btn btn-primary">Update Kanji</button>
        <a href="/kanji" class="btn btn-secondary">Cancel</a>
      </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
  </body>
</html>
